## Data frames are used to store tabular data.
## DF are represented as a special kind of list.
## Data frames are usually created using read.table() or read.csv()

## create a data frame!
x <- data.frame(foo=1:4, bar = c(T,T,F,T))
x
